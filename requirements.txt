# Этот файл генерируется из pyproject.toml через: uv pip compile pyproject.toml -o requirements.txt
# Для разработки используй: uv sync
# Для прод-сборки Docker использует pyproject.toml напрямую через uv

litestar[standard,prometheus]>=2.12
uvicorn[standard]>=0.30
sqlalchemy[asyncio]>=2.0
asyncpg>=0.29
alembic>=1.13
pydantic>=2.7
pydantic-settings>=2.3
httpx>=0.27
celery[redis]>=5.4
redis>=5.0
pyyaml>=6.0
python-dotenv>=1.0
python-json-logger>=2.0
